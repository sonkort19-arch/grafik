(()=>{
  "use strict";

  const ORDER_API="https://yedzfmibceboncrytbqz.supabase.co/functions/v1/ma-crm-order-api";
  const API_KEY="sb_publishable_tSqbw3aeAgxYuzHhQurCuw_yDze4ZNn";
  const ADMIN_SESSION_KEY="ma_schedule_admin_session_v1";
  const CRM_SESSION_KEY="ma_crm_session_v1";
  const $=id=>document.getElementById(id);
  const esc=value=>String(value??"").replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[ch]));
  const num=value=>{const n=Number(String(value??0).replace(/\s/g,"").replace(",","."));return Number.isFinite(n)?n:0;};
  const money=value=>new Intl.NumberFormat("ru-RU",{maximumFractionDigits:2}).format(num(value))+" ₽";
  const readJson=key=>{try{return JSON.parse(localStorage.getItem(key)||"null");}catch(_){return null;}};
  const adminToken=()=>readJson(ADMIN_SESSION_KEY)?.access_token||"";
  const crmSession=()=>{try{return localStorage.getItem(CRM_SESSION_KEY)||"";}catch(_){return"";}};
  let currentRepairId="",sectionRegistered=false;

  function headers(){const h={"Content-Type":"application/json","apikey":API_KEY};const a=adminToken(),s=crmSession();if(a)h.Authorization=`Bearer ${a}`;if(s)h["x-crm-session"]=s;return h;}
  function toast(message){const root=$("crmToast");if(!root)return;root.textContent=String(message||"");root.classList.add("show");setTimeout(()=>root.classList.remove("show"),2800);}
  async function orderApi(op,payload={},options={}){const res=await fetch(ORDER_API,{method:"POST",headers:headers(),body:JSON.stringify({op,...payload}),signal:options.signal,cache:"no-store"});const data=await res.json().catch(()=>({ok:false,error:"Сервер вернул непонятный ответ"}));if(!res.ok||data?.ok===false){const error=new Error(data?.error||`Ошибка ${res.status}`);error.status=res.status;throw error;}return data;}
  function localDateInputToIso(value){if(!value)return null;const d=new Date(value);return Number.isNaN(d.getTime())?null:d.toISOString();}
  function isoToLocalInput(value){if(!value)return"";const d=new Date(value);if(Number.isNaN(d.getTime()))return"";const pad=n=>String(n).padStart(2,"0");return`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`;}
  function dateTime(value){if(!value)return"Не указан";const d=new Date(value);if(Number.isNaN(d.getTime()))return"Не указан";return new Intl.DateTimeFormat("ru-RU",{timeZone:"Europe/Moscow",day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"}).format(d);}
  function dateOnly(value){if(!value)return"—";const d=new Date(value);if(Number.isNaN(d.getTime()))return"—";return new Intl.DateTimeFormat("ru-RU",{timeZone:"Europe/Moscow",day:"2-digit",month:"2-digit",year:"numeric"}).format(d);}
  function methodLabel(method){return({cash:"Наличные",card:"Карта",transfer:"Перевод",other:"Другое"})[method]||method||"—";}
  function categoryLabel(category,kind){return({deposit:"Предоплата",payment:"Оплата",additional:"Доплата",final:"Оплата при выдаче",refund:"Возврат",deposit_refund:"Возврат предоплаты"})[category]||(kind==="refund"?"Возврат":"Оплата");}
  function typeOf(item){return item.display_type||item.item_type||"service";}

  function injectNewRepairFields(){
    const form=$("repairForm");if(!form||$("phase1CreateFields"))return;
    const block=document.createElement("div");block.className="form-section phase1-create";block.id="phase1CreateFields";
    block.innerHTML=`<h3>Срок, гарантия и предоплата</h3><div class="form-grid two"><label><span>Обещанный срок</span><input id="repairDueAt" type="datetime-local"></label><label><span>Гарантия, дней</span><input id="repairWarrantyDays" type="number" min="0" max="730" step="1" value="14"></label><label><span>Предоплата, ₽</span><input id="repairInitialPayment" inputmode="decimal" placeholder="0"></label><label><span>Способ предоплаты</span><select id="repairInitialPaymentMethod"><option value="cash">Наличные</option><option value="card">Карта</option><option value="transfer">Перевод</option><option value="other">Другое</option></select></label></div><label class="form-full"><span>Условия гарантии</span><textarea id="repairWarrantyNote" rows="2" placeholder="Например: гарантия на установленную деталь и работу"></textarea></label>`;
    form.insertBefore(block,$("repairFormError")||null);
  }

  function employeeOptions(value=""){const source=$("repairMaster"),names=[];if(source){for(const opt of source.options){const v=String(opt.value||"").trim();if(v&&!names.includes(v))names.push(v);}}if(value&&!names.includes(value))names.unshift(value);return'<option value="">Исполнитель не указан</option>'+names.map(name=>`<option value="${esc(name)}" ${name===value?"selected":""}>${esc(name)}</option>`).join("");}
  function itemRows(items){
    if(!items.length)return'<div class="phase-empty">Позиции ещё не добавлены</div>';
    return items.map(x=>{const type=typeOf(x),qty=num(x.quantity),price=num(x.unit_price),cost=num(x.unit_cost),discount=num(x.discount_amount),revenue=qty*price-discount,totalCost=qty*cost,profit=revenue-totalCost;return`<div class="phase-line"><div><span class="phase-tag ${type==="part"?"part":"service"}">${type==="part"?"Запчасть":"Услуга"}</span><b>${esc(x.title)}</b><small>${esc(qty)} × ${esc(money(price))}${discount>0?` · скидка ${esc(money(discount))}`:""}${cost>0?` · себестоимость ${esc(money(totalCost))}`:""}${x.executor?` · ${esc(x.executor)}`:""} · <span class="service-profit">прибыль ${esc(money(profit))}</span></small></div><div class="phase-line-sum"><b>${esc(money(revenue))}</b><button type="button" class="phase-icon-danger" data-phase-delete-item="${esc(x.id)}" aria-label="Удалить позицию">×</button></div></div>`;}).join("");
  }
  function paymentRows(payments){if(!payments.length)return'<div class="phase-empty">Платежей пока нет</div>';return payments.map(x=>`<div class="phase-line"><div><span class="phase-tag ${x.kind==="refund"?"refund":"payment"}">${esc(categoryLabel(x.category,x.kind))}</span><b>${esc(methodLabel(x.method))}</b><small>${esc(dateTime(x.created_at))}${x.note?` · ${esc(x.note)}`:""}${x.created_by?` · ${esc(x.created_by)}`:""}</small></div><div class="phase-line-sum ${x.kind==="refund"?"negative":""}"><b>${x.kind==="refund"?"−":"+"}${esc(money(x.amount))}</b></div></div>`).join("");}
  function totalsOf(data){if(data?.totals)return data.totals;const items=data.items||[],payments=data.payments||[],itemsTotal=Math.round(items.reduce((s,x)=>s+num(x.quantity)*num(x.unit_price)-num(x.discount_amount),0)*100)/100,cost=Math.round(items.reduce((s,x)=>s+num(x.quantity)*num(x.unit_cost),0)*100)/100,paid=Math.round(payments.reduce((s,x)=>s+(x.kind==="refund"?-1:1)*num(x.amount),0)*100)/100,orderTotal=items.length?itemsTotal:num(data.repair?.final_price??data.repair?.estimated_price??0);return{itemsTotal,cost,profit:Math.round((itemsTotal-cost)*100)/100,paid,orderTotal,balance:Math.round((orderTotal-paid)*100)/100};}

  function panelHtml(data){
    const base=window.MAOrderController?.repair||{},r={...base,...(data.repair||{})},items=data.items||[],payments=data.payments||[],t=totalsOf(data),warrantyState=r.warranty_days>0?(r.warranty_until?`до ${dateOnly(r.warranty_until)}`:`${r.warranty_days} дн., начнётся после выдачи`):"Без гарантии";
    return `<div id="phase1Root" class="phase1-root" data-repair-id="${esc(r.id||currentRepairId)}"><div class="detail-panel phase-summary-panel"><div class="phase-title-row"><div><h3>Срок и гарантия</h3><p>Контроль обещанного срока и условий</p></div></div><div class="edit-grid phase-meta-grid"><label><span>Обещанный срок</span><input id="phaseDueAt" type="datetime-local" value="${esc(isoToLocalInput(r.due_at))}"></label><label><span>Гарантия, дней</span><input id="phaseWarrantyDays" type="number" min="0" max="730" step="1" value="${esc(r.warranty_days??14)}"></label><label class="form-full"><span>Условия гарантии</span><textarea id="phaseWarrantyNote" rows="2">${esc(r.warranty_note||"")}</textarea></label></div><div class="phase-inline-facts"><span><small>Срок</small><b>${esc(dateTime(r.due_at))}</b></span><span><small>Гарантия</small><b>${esc(warrantyState)}</b></span></div><div class="detail-edit-actions"><button class="btn btn-secondary" id="phaseSaveMeta" type="button">Сохранить срок и гарантию</button></div></div><div class="detail-panel"><div class="phase-title-row"><div><h3>Товары и услуги</h3><p>Цена, себестоимость, скидка, исполнитель и прибыль</p></div><b>${esc(money(t.itemsTotal))}</b></div><div class="phase-lines" id="phaseItemRows">${itemRows(items)}</div><form class="phase-add-form" id="phaseItemForm"><select id="phaseItemType"><option value="service">Услуга</option><option value="part">Товар / запчасть</option></select><input id="phaseItemTitle" required placeholder="Название"><input id="phaseItemQty" required inputmode="decimal" value="1" placeholder="Кол-во"><input id="phaseItemPrice" required inputmode="decimal" placeholder="Цена клиенту, ₽"><input id="phaseItemCost" inputmode="decimal" placeholder="Себестоимость, ₽"><input id="phaseItemDiscount" inputmode="decimal" placeholder="Скидка, ₽"><select id="phaseItemExecutor">${employeeOptions(r.master||"")}</select><input id="phaseItemServiceCatalogId" type="hidden"><input id="phaseItemInventoryProductId" type="hidden"><button class="btn btn-primary" type="submit">+ Добавить</button></form><div class="service-cost-preview" id="serviceCostPreview"><span>(Цена × количество − скидка) − себестоимость</span><b>Прибыль: 0 ₽</b></div><div class="phase-money-grid"><span><small>Сумма позиций</small><b>${esc(money(t.itemsTotal))}</b></span><span><small>Себестоимость</small><b>${esc(money(t.cost))}</b></span><span class="service-profit-total"><small>Прибыль по позициям</small><b>${esc(money(t.profit))}</b></span></div></div><div class="detail-panel"><div class="phase-title-row"><div><h3>Платежи</h3><p>Отдельные операции, связанные с заказом и кассой</p></div></div><div class="phase-money-grid"><span><small>Сумма заказа</small><b>${esc(money(t.orderTotal))}</b></span><span><small>Оплачено</small><b>${esc(money(t.paid))}</b></span><span class="${t.balance>0?"attention":"ok"}"><small>Остаток</small><b>${esc(money(t.balance))}</b></span></div><div class="phase-lines" id="phasePaymentRows">${paymentRows(payments)}</div><form class="phase-add-form payment-form" id="phasePaymentForm"><select id="phasePaymentCategory"><option value="deposit">Предоплата</option><option value="payment" selected>Оплата</option><option value="additional">Доплата</option><option value="refund">Возврат</option><option value="deposit_refund">Возврат предоплаты</option></select><select id="phasePaymentMethod"><option value="cash">Наличные</option><option value="card">Карта</option><option value="transfer">Перевод</option><option value="other">Другое</option></select><input id="phasePaymentAmount" required inputmode="decimal" placeholder="Сумма, ₽"><input id="phasePaymentNote" placeholder="Комментарий"><button class="btn btn-primary" type="submit">Записать</button></form></div></div>`;
  }

  function patchVisibleAmount(repairId,total){document.querySelectorAll(`[data-repair-id="${CSS.escape(String(repairId))}"]`).forEach(root=>{const amount=root.querySelector(".amount-cell")||root.querySelector(".mobile-row-meta b");if(amount)amount.textContent=money(total);});const final=$("detailFinal");if(final)final.value=money(total);}
  function updatePreview(){const price=num($("phaseItemPrice")?.value),cost=num($("phaseItemCost")?.value),qty=Math.max(0,num($("phaseItemQty")?.value)||0),discount=num($("phaseItemDiscount")?.value),profit=price*qty-discount-cost*qty,out=$("serviceCostPreview");if(out)out.innerHTML=`<span>(Цена × количество − скидка) − себестоимость</span><b>Прибыль: ${esc(money(profit))}</b>`;}
  function commerceLoader({repairId,signal}){return orderApi("commerce",{repairId},{signal});}
  function ensureSection(){const controller=window.MAOrderController;if(sectionRegistered||!controller)return;controller.registerSection("commerce",commerceLoader);sectionRegistered=true;}

  function renderCommerce(data){
    const repairId=String(data?.repair?.id||window.MAOrderController?.repairId||"");if(!repairId)return;currentRepairId=repairId;
    const body=$("repairDetailBody");if(!body?.querySelector(".order-layout"))return;
    body.querySelector("#phase1Root")?.remove();const main=body.querySelector(".order-main")||body.querySelector(".order-layout")||body,wrap=document.createElement("div");wrap.innerHTML=panelHtml(data);main.appendChild(wrap.firstElementChild);
    bind(repairId);const totals=totalsOf(data);patchVisibleAmount(repairId,totals.orderTotal);document.dispatchEvent(new CustomEvent("ma:order:phase-ready",{detail:{repairId,token:window.MAOrderController?.token||0,data,totals}}));
  }

  async function refreshCommerce(){ensureSection();try{return await window.MAOrderController?.refreshSection("commerce",{force:true});}catch(e){console.warn("MA CRM commerce",e);toast(e.message||"Данные товаров и платежей временно недоступны");return null;}}
  function clearItemForm(form){for(const id of ["phaseItemTitle","phaseItemPrice","phaseItemCost","phaseItemDiscount","phaseItemServiceCatalogId","phaseItemInventoryProductId"]){const el=$(id);if(el)el.value="";}if($("phaseItemQty"))$("phaseItemQty").value="1";delete form.dataset.idempotencyKey;updatePreview();}

  function bind(repairId){
    $("phaseSaveMeta")?.addEventListener("click",async e=>{const btn=e.currentTarget;btn.disabled=true;try{await orderApi("update-order",{id:repairId,dueAt:localDateInputToIso($("phaseDueAt")?.value||""),warrantyDays:Number($("phaseWarrantyDays")?.value||14),warrantyNote:$("phaseWarrantyNote")?.value||""});toast("Срок и гарантия сохранены");await window.MAOrderController?.refresh({reason:"meta",keepMarkup:true});}catch(err){toast(err.message||"Не удалось сохранить");}finally{btn.disabled=false;}});
    const itemForm=$("phaseItemForm");itemForm?.addEventListener("input",e=>{if(e.target?.id!=="phaseItemServiceCatalogId"&&e.target?.id!=="phaseItemInventoryProductId")delete itemForm.dataset.idempotencyKey;updatePreview();});
    itemForm?.addEventListener("submit",async e=>{e.preventDefault();const form=e.currentTarget,btn=form.querySelector('button[type="submit"]');if(btn.disabled)return;btn.disabled=true;const key=form.dataset.idempotencyKey||(form.dataset.idempotencyKey=crypto.randomUUID());try{const result=await orderApi("upsert-item",{repairId,itemType:$("phaseItemType")?.value||"service",title:$("phaseItemTitle")?.value||"",quantity:$("phaseItemQty")?.value||1,unitPrice:$("phaseItemPrice")?.value||0,unitCost:$("phaseItemCost")?.value||0,discount:$("phaseItemDiscount")?.value||0,executor:$("phaseItemExecutor")?.value||"",serviceCatalogId:$("phaseItemServiceCatalogId")?.value||"",inventoryProductId:$("phaseItemInventoryProductId")?.value||"",idempotencyKey:key});toast(result.duplicate?"Позиция уже была добавлена":"Позиция добавлена");window.MAOrderController?.updateSnapshot({final_price:result.finalPrice});patchVisibleAmount(repairId,result.finalPrice||0);clearItemForm(form);await refreshCommerce();}catch(err){toast(err.message||"Не удалось добавить позицию");}finally{btn.disabled=false;}});
    document.querySelectorAll("#phase1Root [data-phase-delete-item]").forEach(btn=>btn.addEventListener("click",async()=>{if(!confirm("Удалить эту позицию из заказа?"))return;if(btn.disabled)return;btn.disabled=true;try{const result=await orderApi("delete-item",{repairId,id:btn.dataset.phaseDeleteItem});toast("Позиция удалена");window.MAOrderController?.updateSnapshot({final_price:result.finalPrice});if(result.finalPrice!==null)patchVisibleAmount(repairId,result.finalPrice||0);await refreshCommerce();}catch(err){toast(err.message||"Не удалось удалить позицию");btn.disabled=false;}}));
    const paymentForm=$("phasePaymentForm");paymentForm?.addEventListener("input",()=>{delete paymentForm.dataset.idempotencyKey;});
    paymentForm?.addEventListener("submit",async e=>{e.preventDefault();const form=e.currentTarget,btn=form.querySelector('button[type="submit"]');if(btn.disabled)return;btn.disabled=true;const category=$("phasePaymentCategory")?.value||"payment",kind=category.includes("refund")?"refund":"payment",key=form.dataset.idempotencyKey||(form.dataset.idempotencyKey=crypto.randomUUID());try{const result=await orderApi("add-payment",{repairId,kind,category,method:$("phasePaymentMethod")?.value||"cash",amount:$("phasePaymentAmount")?.value||0,note:$("phasePaymentNote")?.value||"",idempotencyKey:key});toast(result?.result?.duplicate?"Операция уже записана":kind==="refund"?"Возврат записан":"Оплата записана");if($("phasePaymentAmount"))$("phasePaymentAmount").value="";if($("phasePaymentNote"))$("phasePaymentNote").value="";delete form.dataset.idempotencyKey;await refreshCommerce();}catch(err){toast(err.message||"Не удалось записать оплату");}finally{btn.disabled=false;}});
    updatePreview();
  }

  document.addEventListener("ma:order:ready",e=>{const id=e.detail?.repair?.id||e.detail?.repairId;if(!id)return;currentRepairId=String(id);ensureSection();refreshCommerce();});
  document.addEventListener("ma:order:section-loaded",e=>{if(e.detail?.section!=="commerce")return;renderCommerce(e.detail.data);});
  document.addEventListener("ma:order:closed",()=>{currentRepairId="";});

  injectNewRepairFields();
  window.MAOrderPhase={refresh:refreshCommerce,get currentRepairId(){return currentRepairId;}};
})();
