name: Build final MA icon assets

on:
  push:
    branches: [icon-refresh-v3]
    paths:
      - 'ma-icon-final-v5.svg'
      - '.github/workflows/icon-v4-build.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-icon-assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure ImageMagick
        run: |
          if ! command -v convert >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y imagemagick
          fi
      - name: Generate required icon sizes
        run: |
          convert -background none ma-icon-final-v5.svg -resize 192x192 ma-icon-final-v5-192.png
          convert -background none ma-icon-final-v5.svg -resize 512x512 ma-icon-final-v5-512.png
          convert -background none ma-icon-final-v5.svg -resize 180x180 ma-apple-touch-final-v5.png
          convert -background none ma-icon-final-v5.svg -resize 64x64 ma-favicon-final-v5.png
          identify ma-icon-final-v5-192.png ma-icon-final-v5-512.png ma-apple-touch-final-v5.png ma-favicon-final-v5.png
      - name: Commit generated assets
        run: |
          git config user.name "MA Icon Builder"
          git config user.email "actions@users.noreply.github.com"
          git add ma-icon-final-v5-192.png ma-icon-final-v5-512.png ma-apple-touch-final-v5.png ma-favicon-final-v5.png
          if git diff --cached --quiet; then
            echo "Icon assets already current"
            exit 0
          fi
          git commit -m "Generate clean MA icon assets"
          git push
