name: Inspect employee step 5

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/inspect-employee-step5.yml

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inspect employee functions
        run: python3 tools/inspect_employee_step5.py
